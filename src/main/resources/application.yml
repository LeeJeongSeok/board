spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/board?allowPublicKeyRetrieval=true&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 1q2w3e4r

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: true

logging:
  level:
    root: info
